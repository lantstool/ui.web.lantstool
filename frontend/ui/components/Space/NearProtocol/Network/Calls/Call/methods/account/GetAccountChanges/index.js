import { defaultValues } from './defaultValues.js';
import { GetAccountChanges } from './GetAccountChanges.jsx';

export const getAccountChanges = {
  defaultValues,
  component: GetAccountChanges,
};
