import { setAppSettings } from './setAppSettings.js';
import { resetState } from './resetState.js';
import { setNotification } from './setNotification.js';
import { setMigrations } from './setMigrations.js';

export const actions = {
  setAppSettings,
  resetState,
  setNotification,
  setMigrations,
};
